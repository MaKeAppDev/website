version: '3.7'

services:
  website:
    build:
      context: ./
      dockerfile: .devcontainer/Dockerfile
    volumes:
      - '.:/app'
    working_dir: '/app'
    ports:
      - '3000:3000'
    command: sh -c "npm install && npm run serve"
